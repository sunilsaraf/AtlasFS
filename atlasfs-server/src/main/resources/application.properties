# Server configuration
server.port=8080
server.host=0.0.0.0

# Storage backend
storage.backend=rocksdb
storage.path=/var/lib/atlasfs/metadata

# Performance tuning
cache.size.mb=512
thread.pool.size=64

# Logging
logging.level.root=INFO
logging.level.com.atlasfs=DEBUG
